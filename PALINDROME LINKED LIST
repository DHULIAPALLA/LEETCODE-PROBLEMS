class Solution:
    def isPalindrome(self, head: Optional[ListNode]) -> bool:
        temp=head
        ls=[]
        while temp:
            ls.append(temp.val)
            temp=temp.next
        if ls==ls[::-1]:
            return True
        else:
            return False
        
