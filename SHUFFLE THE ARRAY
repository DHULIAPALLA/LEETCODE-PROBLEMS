class Solution:
    def shuffle(self, nums: List[int], n: int) -> List[int]:
        res=[]
        index=0
        while index<n:
            res.extend([nums[index],nums[n+index]])
            index=index+1
        return res
        
        
